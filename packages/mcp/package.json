{
  "name": "@proteus/mcp",
  "version": "0.0.1",
  "description": "MCP protocol adapter for Proteus — client, server, and tool translation",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@proteus/core": "workspace:*",
    "zod": "^3.25.0",
    "zod-to-json-schema": "^3.25.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.1.0"
  }
}
